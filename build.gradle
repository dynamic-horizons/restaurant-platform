subprojects {
    apply plugin: 'java'

    repositories {
       mavenCentral()
    }

	dependencies {
		testCompile (
			'junit:junit:4.10',
	    )
	}

    version = '1.0'

    jar {
        manifest.attributes provider: 'Dynamic Horizons, LLC'
    }
}